{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Bienvenue sur le site de Coach Ibra</h1>
        
        {# Liens vers les pages de profil et de connexion #}
        <div class="mt-3">
            <a href="{{ path('app_profile') }}" class="btn btn-info">Voir mon profil</a>
            <a href="{{ path('app_login') }}" class="btn btn-secondary">Se connecter</a>
        </div>
    </div>
{% endblock %}
